spring.application.name=rocketmq-bus-node3
server.port=10003
management.endpoints.web.exposure.include=bus-env,httptrace
spring.cloud.bus.id=${spring.application.name}:${server.port}
spring.cloud.bus.trace.enabled=true